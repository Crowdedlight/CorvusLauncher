


// TODO
//  launch selected amount of HCs. Require that A3 server process is running first, So check for that and report error if not. (As only the A3 server launch ensure we have gone through process with keys and mods)
//  Build commandline string for HC, PAR file, Profile, set as Client
//  Launch process as child and disown